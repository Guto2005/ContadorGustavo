import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  themeToggle: {
    marginBottom: 20,
  },
  toggleText: {
    fontSize: 18,
    color: 'blue',
  },
});

export default styles;
