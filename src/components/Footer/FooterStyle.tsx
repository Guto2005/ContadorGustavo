import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    position: 'absolute',
    alignItems: 'center',
    bottom: 10,
  },
  footerText: {
    fontSize: 14,
  },
});

export default styles;
