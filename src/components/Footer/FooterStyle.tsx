import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    position: 'absolute',
    bottom: 10,
  },
  footerText: {
    fontSize: 14,
  },
});

export default styles;
