import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  counterContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },
  countText: {
    fontSize: 32,
    marginHorizontal: 20,
  },
});

export default styles;
